<h3>Edit User</h3>
<%= render 'error' %>
<%= form_for @user, url: user_path(@user.id), method: :put do |f| %>

<%= render 'form', f: f %>
  <br><br>
  <%= f.submit 'Update User' %>
<% end %>
<%= link_to ' user',  users_path %>
